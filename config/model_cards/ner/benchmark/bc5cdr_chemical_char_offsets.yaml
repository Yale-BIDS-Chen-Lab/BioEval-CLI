# ------------------------------
#  Task and Dataset Settings
# ------------------------------
task: ner 
dataset: bc5cdr_chemical_char_offsets  # Dataset identifier/name for organizing and querying experiments

data_files:
  test_file: "data/datasets/samples/bc5cdr_chemical_char_offsets_test_samples.json"
  train_file:

# ------------------------------
#  Prompt Generation Settings
# ------------------------------
prompt:
  system_prompt: "config/prompts/ner/bc5cdr_chemical.txt"
  shot_indices:  # Omit for zero-shot, or "0,1,2" for 3-shot
  output_file: null  # Will be auto-named as bc5cdr_chemical_char_offsets_[hash].json. All pipeline stages update this single file by default.

# ------------------------------
#  Model Inference Settings
# ------------------------------
model:
  provider: hf
  name: "meta-llama/Llama-3.2-3B-Instruct"

inference:
  temperature: 0.01
  max_new_tokens: 200
  output_file: null  # Will be auto-named as bc5cdr_chemical_char_offsets_[hash].json. All pipeline stages update this single file by default.

# ------------------------------
#  Evaluation Settings
# ------------------------------
evaluation:
  postprocessing: process_ner_char_offsets  # Extract entities from HTML-annotated text with span tags (CHARACTER OFFSETS)
  labels: chemical                          # Entity type to extract
  output_file: null  # Will be auto-named as bc5cdr_chemical_char_offsets_[hash].json. All pipeline stages update this single file by default.
  metrics:
    - exact_match_precision
    - exact_match_recall
    - exact_match_f1

